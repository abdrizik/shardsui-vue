name: Docs CI

on:
  push:
    branches: [main]
    paths:
      - docs/**
      - packages/shardsui/src/**
      - packages/shardsui/package.json
      - pnpm-lock.yaml
      - pnpm-workspace.yaml
      - .github/workflows/docs-ci.yml
  pull_request:
    paths:
      - docs/**
      - packages/shardsui/src/**
      - packages/shardsui/package.json
      - pnpm-lock.yaml
      - pnpm-workspace.yaml
      - .github/workflows/docs-ci.yml

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  docs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: pnpm/action-setup@v6
        with:
          version: 12

      - uses: actions/setup-node@v6
        with:
          node-version: lts/*
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - run: pnpm lint

      - run: pnpm --filter @shardsui/vue build

      - run: pnpm --filter docs check

      - run: pnpm --filter docs build
