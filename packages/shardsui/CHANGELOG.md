# Changelog

## 0.1.0-beta.1

- `as` now takes a component as well as a tag name, so a part can render your own button or your
  router's link instead of the element it picks by default.

## 0.1.0-beta.0

- Initial beta release.
