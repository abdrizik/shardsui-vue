<script setup lang="ts">
import { DirectionProvider } from '@/components/direction-provider'
import { ScrollArea } from '@/components/scroll-area'

const { orientation = 'vertical', direction = 'ltr' } = defineProps<{
  orientation?: 'horizontal' | 'vertical'
  direction?: 'ltr' | 'rtl'
}>()
</script>

<template>
  <DirectionProvider :direction="direction">
    <ScrollArea.Root :style="`width: 200px; height: 200px; direction: ${direction};`">
      <ScrollArea.Viewport data-testid="viewport" style="width: 100%; height: 100%">
        <div
          :style="{
            width: orientation === 'horizontal' ? '1000px' : '100%',
            height: orientation === 'vertical' ? '1000px' : '100%'
          }"
        ></div>
      </ScrollArea.Viewport>
      <ScrollArea.Scrollbar :orientation="orientation" data-testid="scrollbar" keep-mounted>
        <ScrollArea.Thumb data-testid="thumb" />
      </ScrollArea.Scrollbar>
    </ScrollArea.Root>
  </DirectionProvider>
</template>
